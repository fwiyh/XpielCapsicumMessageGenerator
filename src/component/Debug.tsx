import React, { useContext } from "react";

type ParamType = {
    // Region Index
    regionIndexes: number[],
}

export const Debug = (params: ParamType) => {
    
    // findNode
    
    // calcEachNodes
    
    // calcTop_N_Nodes
    
    
    return (
        <div className="row">
        
        </div>
    )
}